import type { Metadata } from "next";
import { ExperienceSection } from "@/components/Sections";

export const metadata: Metadata = { title: "Experience — Kunta Mallik Raj" };

export default function Page() {
  return <ExperienceSection />;
}
