import type { Metadata } from "next";
import { AboutSection } from "@/components/Sections";

export const metadata: Metadata = { title: "About & Skills — Kunta Mallik Raj" };

export default function Page() {
  return <AboutSection />;
}
