import type { Metadata } from "next";
import { OpenSourceSection } from "@/components/Sections";

export const metadata: Metadata = { title: "Open Source — Kunta Mallik Raj" };

export default function Page() {
  return <OpenSourceSection />;
}
