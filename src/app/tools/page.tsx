import type { Metadata } from "next";
import { ToolsSection } from "@/components/Sections";

export const metadata: Metadata = { title: "Developer Tools — Kunta Mallik Raj" };

export default function Page() {
  return <ToolsSection />;
}
