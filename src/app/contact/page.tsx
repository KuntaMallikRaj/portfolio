import type { Metadata } from "next";
import { ContactSection } from "@/components/Sections";

export const metadata: Metadata = { title: "Contact — Kunta Mallik Raj" };

export default function Page() {
  return <ContactSection />;
}
