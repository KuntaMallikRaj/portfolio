import type { Metadata } from "next";
import { ProjectsSection } from "@/components/Sections";

export const metadata: Metadata = { title: "Projects — Kunta Mallik Raj" };

export default function Page() {
  return <ProjectsSection />;
}
