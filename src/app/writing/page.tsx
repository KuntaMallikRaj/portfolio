import type { Metadata } from "next";
import { WritingSection } from "@/components/Sections";

export const metadata: Metadata = { title: "Technical Writing — Kunta Mallik Raj" };

export default function Page() {
  return <WritingSection />;
}
